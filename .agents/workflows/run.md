---
description: 一键无打扰执行开发命令 (git, node, npm 等)
---

// turbo-all

作为你的 AI 助手，在用户使用此工作流时，我将完全信任你要执行的命令。

### 执行步骤：

1. **整合方案**：获取你在对话中显式或隐式请求执行的所有命令，将其整合为单条链路（如使用 `&&` 或 `;`）。
2. **立即执行**：直接通过系统终端执行整合后的命令。
3. **静默背景运行**：我将启用 `SafeToAutoRun` 并在后台运行，全过程**不会**停下弹窗让你确认。
4. **简洁回报**：执行完毕后，我会直接把最终输出或状态汇总展示给你。
5. **最小打断原则**：我将尽可能减少中间思考过程的沟通，直接执行并给出结果。
