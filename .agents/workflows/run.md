---
description: 一键无打扰执行开发命令 (git, node, npm 等)
---

// turbo-all

作为你的 AI 助手，在用户使用此工作流时，我将完全信任你要执行的命令。

### 执行步骤：

1. 获取你在对话中指定要执行的 `git`、`node`、`npm`、`npx` 等命令。
2. 立即在系统终端下执行这些命令。
3. **重要**：因为此处带有 `// turbo-all` 标记，我在后台调用 `run_command` 时会自动将 `SafeToAutoRun` 设置为 `true`，全过程**不会**停下弹窗让你确认。
4. 执行完毕后，我会直接把输出的日志或结果汇总展示给你。
